//! mcp-bus core: envelope, registry, mailbox, router, eventlog.

pub mod envelope;
pub mod ids;
pub mod mailbox;
pub mod registry;
pub mod router;

// Modules will be added in later tasks.
// pub mod eventlog;
